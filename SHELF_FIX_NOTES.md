Silva OS 3.9.8 shelf-fix v13

This package fixes the real live Home renderer by making shelf_fix_v10.js own window.renderHomes and marking the new focused/all-team layouts as alpha-home-system-shell so older alpha code stops re-rendering the old vertical home page.
