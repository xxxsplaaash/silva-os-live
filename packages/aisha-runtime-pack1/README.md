# runtime_pack1

Pack 1 executable scaffold target.

Now includes:
- config and core schemas
- accepted memory substrate starter files
- accepted runtime transaction / parser / validator / fallback starter files

Still missing:
- `processTurn.ts` orchestration
- real persistence layer
- generator adapter wiring
- state engine starter files
- evaluation harness starter files

Status:
scaffold only, not full implementation.
